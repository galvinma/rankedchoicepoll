import axios from 'axios';

// functions
import { retMinMax, tally, merge, iterateTally } from './tallyvotes'


it('correctly tallies a simple poll', () => {
  let poll_items = ['apple', 'pear', 'banana', 'grape', 'strawberry', 'guava', 'peach']
  let votes = []

});
